# 农药库存管理需求清单

本清单面向当前“农药 / 农资库存 + AI 助手”版本，目标是把系统从可查询的库存表升级成可追溯、可盘点、可用于生产记录的农资管理工具。

## R1. 库存流水 / 出入库台账

状态：已进入实现

### 背景

当前系统已经支持通过 AI 口令扣减库存，但只保存最终库存数量。实际农药管理需要知道每一次库存变化的来源，尤其是采购入库、领用出库、盘点修正、删除处理等操作。

### 功能范围

- 每次新增产品时生成一条“新增库存”流水。
- 每次手动编辑库存数量时生成一条“库存调整”流水。
- 每次通过 AI 扣减库存时生成一条“AI 出库”流水。
- 删除产品前生成一条“删除产品”流水，保留产品名称快照。
- 产品详情页展示该产品最近库存流水。

### 流水字段

- 产品 ID
- 产品名称快照
- 操作类型：新增、出库、调整、删除
- 变更前数量
- 变更后数量
- 变化量
- 来源：手动、AI、导入
- 原因 / 备注
- 创建时间

### 验收标准

- 新增、编辑、AI 扣减、删除产品都会写入数据库流水表。
- 产品详情页能看到最近的库存流水。
- 删除产品后流水不丢失，产品 ID 可置空但产品名称快照保留。
- 测试覆盖后端写入与前端展示。

## R2. 批次管理

状态：部分实现

### 背景

同一种农药可能多次采购，批号、有效期、规格、剩余数量和存放位置不同。如果只在产品级别保存一个数量，会导致近效期管理不准确。

### 功能范围

- 一个产品下可有多个批次。
- 批次字段包括批号、生产日期、有效期、数量、采购日期、供应商、位置。
- 产品详情页展示批次列表与批次数量汇总。
- 已实现：批次新增、编辑接口、删除接口、详情页新增与删除、批次数量汇总、批次变化写入库存流水。
- 待实现：出库时默认优先扣减近效期批次。

### 验收标准

- 支持同一产品多个批次。
- 出库能按近效期优先扣减。
- 快过期提醒以批次为单位。

## R3. 农药标签识别增强

状态：待实现

### 背景

农药包装标签包含大量结构化信息，AI 最适合做标签 OCR + 信息抽取，但不能臆造剂量和安全间隔期。

### 功能范围

- 上传包装照片识别：
  - 农药登记证号
  - 有效成分
  - 含量
  - 剂型
  - 毒性等级
  - 生产厂家
  - 防治对象
  - 适用作物
  - 安全间隔期
  - 标签用量
  - 注意事项
- 不确定字段留空。
- 明确提示“仅从标签识别，不补全不存在的信息”。

### 验收标准

- 图片录入能自动填充主要标签字段。
- AI 不编造安全间隔期和用量。
- 用户能在保存前逐项确认。

## R4. 地块 / 作物用药记录

状态：待实现

### 背景

库存管理只能回答“仓库里有什么”，生产管理还需要回答“哪个地块什么时候用了什么”。

### 功能范围

- 建立地块档案。
- 建立作物档案。
- 记录一次用药：
  - 地块
  - 作物
  - 病虫草害
  - 使用产品
  - 使用数量
  - 用药日期
  - 操作人
  - 天气
  - 备注
- 用药记录可自动扣库存并写入库存流水。

### 验收标准

- 能按地块、作物、时间查询用药历史。
- 用药记录与库存扣减联动。
- 能统计某个作物周期内使用过哪些农药。

## R5. 低库存 / 近效期 / 风险提醒

状态：待实现

### 背景

农药库存管理最常见的实际问题是“快用完了没发现”和“过期了才发现”。

### 功能范围

- 每个产品可设置安全库存线。
- 低库存提醒。
- 近效期提醒。
- 已过期隔离提醒。
- 长期未盘点提醒。
- 同类库存过高提醒。

### 验收标准

- 首页或库存列表能看到提醒。
- 通知服务可推送低库存和近效期摘要。
- AI 能回答“哪些产品需要补货”。

## R6. 盘点模式

状态：待实现

### 背景

实际仓库数量会因为领用漏记、损耗、包装破损产生偏差，需要周期性盘点。

### 功能范围

- 进入盘点模式后逐个产品核对实际数量。
- 输入实际数量后自动计算盘盈 / 盘亏。
- 盘点结果写入库存流水。
- 支持按分类、位置分批盘点。

### 验收标准

- 盘点过程不中断当前库存列表。
- 盘点差异可追溯。
- 完成后生成盘点摘要。

## R7. 成本 / 供应商管理

状态：待实现

### 背景

库存数量之外，采购成本和供应商也会影响经营判断。

### 功能范围

- 记录采购单价、总价、供应商。
- 统计月度采购成本。
- 结合用药记录统计地块 / 作物成本。

### 验收标准

- 能查询某段时间采购成本。
- 能按产品和分类统计库存价值。
- 后续能接入地块成本核算。
