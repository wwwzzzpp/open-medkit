# 系统交接文档 (AI Handover Document)

**目标读者：即将接手将本项目重构为小程序（如微信/支付宝）与多端应用（使用 Flutter / 原生小程序等框架）的下一任 AI 开发助手。**

---

## 1. 项目背景与当前进度

本项目目前是一个基于 Web 技术栈（React + Node.js Hono + SQLite）的**农资（农药、化肥、种子等）库存管理与 AI 智能录入系统**。  
系统拥有完整的闭环流程，用户（所有者）目前准备将其重构为跨平台的小程序，因此需要此文档作为核心逻辑的交接依据。

**当前已完成的功能及进度记录：**
- [x] **多用户认证体系 (Multi-user Auth)**：引入了完整的注册/登录认证流程。基于 Argon2 密码加密算法，并通过 HTTP-Only Cookie 维护 Session，实现了各店铺/用户的农资数据隔离。
- [x] **AI 自然语言解析与录入**：用户只需使用大白话描述进货信息，后台通过调用大语言模型（如 DeepSeek），自动抽取出结构化的农资数据（品名、类别、有效期、规格、数量）。
- [x] **库存与批次精细化追踪**：
  - **`medicines` (农资主表)**：维护农资的基础信息和当前总库存。
  - **`inventory_batches` (批次表)**：管理相同农资在不同时间采购的详细批次信息及过期时间。
  - **`inventory_transactions` (流水记录表)**：记录每一次入库、出库、修改动作的变动前/后数量及操作原因。
- [x] **多渠道消息推送引擎**：集成了 Telegram、Discord、飞书机器人、邮件等多种推送渠道，用于定时巡检并通知用户临期与过期农资。
- [x] **国内云环境深度部署优化**：通过编写全自动脚本（使用 Rsync 直推、配置 Alpine 阿里云镜像等）成功解决了国内服务器拉取 GitHub、安装依赖超时崩溃等经典问题。

---

## 2. 核心架构与数据库设计原语

为了确保在小程序端和新后端（如果需要重写后端）中逻辑保持一致，请下一位 AI 开发者重点留意当前的架构约束：

### 2.1 数据库结构映射 (SQLite)
目前的 SQLite 数据库通过 `better-sqlite3` 驱动，所有的核心表都强关联了 `user_id`。如果在新系统中更换为 MySQL/PostgreSQL，请务必保留这层隔离逻辑。
*   `users`: 包含 `id`, `username`, `password_hash`。当前系统的默认超级管理员会自动初始化为 `admin` / `admin123`。
*   `medicines`: 农资主档案，包含 `user_id`, `name`, `category` (分类：杀虫剂/杀菌剂/除草剂等), `quantity`, `expires_at`。
*   `inventory_transactions`: 库存流水账本，关键字段为 `action_type` (枚举), `quantity_delta` (变动数量), `source` (操作来源)。
*   `inventory_batches`: 批次控制表，用于追溯每一批进货的供应商、生产日期和过期日。
*   `notification_channels` & `app_settings`: 用于保存特定用户的推送机器人配置及 AI API Key 配置。

### 2.2 接口交互设计 (API)
目前的后端基于 Hono 框架，提供了标准的 RESTful API（如 `/api/medicines`、`/api/auth/login` 等）。

---

## 3. 小程序重构：核心挑战与路线建议

当你（下一位 AI）开始着手重构时，请特别关注以下技术点，以防在新环境中“水土不服”：

### 3.1 鉴权机制 (Auth) 改造 【重要】
目前系统采用的是 `Set-Cookie` 并在浏览器端通过 Cookie 传递 Session。**小程序环境对 Cookie 的支持极弱且不稳定**。
**建议动作：** 在重写前后端交互时，请将现有的 Cookie Auth 改造成 **JWT (JSON Web Token)** 机制，或者前端在发起请求时主动在 Header 中附带 `Authorization: Bearer <token>`。

### 3.2 界面交互优化
1.  **语音录入增强**：目前网页版依赖用户输入文字。在小程序端重写时，强烈建议调用小程序原生的“语音转文字” API（如微信同声传译插件），用户说话即可完成 AI 录入，体验会有质的飞跃。
2.  **列表性能**：农资列表、流水列表需要改用小程序的 `scroll-view` 或虚拟列表，支持上拉加载更多和下拉刷新。

### 3.3 通知模块的本地化
目前的推送系统采用了海外主流平台（TG、Discord）以及办公软件（飞书）。在转为小程序生态后，建议优先接入 **微信/支付宝的小程序订阅消息 (Template Messages)**，这将极大简化用户的配置难度，实现直接在微信弹窗提示农药过期的效果。

### 3.4 大模型配置存放策略
当前网页版的做法是将配置存储在浏览器的 `localStorage`。小程序的 `Storage` 虽然可用，但如果是面向公众的 SaaS，建议将 AI 配置统一放到服务器端，或者在 `app_settings` 表里由后台代为调用 LLM，避免前端暴露逻辑。

---
交接完毕。祝你在新版本的重构工作中一切顺利！
